<%= form_with(model: food_item, local: true) do |form| %>
  <% if food_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food_item.errors.count, "error") %> prohibited this food_item from being saved:</h2>

      <ul>
      <% food_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, class: 'form-control-label' %>
    <%= form.text_field :name, class: 'form-control', autofocus: true, placeholder: "e.g. Beef Taco", :required => 'required' %>
</div>

  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, placeholder: "e.g. 15.00", :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :calories %>
    <%= form.text_field :Calories, placeholder: "e.g. 1200", :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :section_id %>
    <%= form.select :section_id, Section.all.map{|e| [e.name, e.id]}, :required => 'required' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
