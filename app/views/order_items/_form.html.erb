<%= form_with(model: order_item, local: true) do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
      <% order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer %>
    <%= form.text_field :customer, placeholder: "e.g. John Smith" %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, :value => params[:cost], :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :food_item_id %>
    <%= form.select :food_item_id, options_from_collection_for_select(FoodItem.order(:name).all, "id", "name", params[:item]), :required => 'required' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
